{% extends 'admin/admin.html.twig' %}

{% block name %}Éditer {{ category.name }}{% endblock %}

{% block body %}
    <h1>Éditer {{ category.name }}</h1>
     
    <div class="form-wrapper">
      
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.save) }}
        {{ form_end(form) }}

    </div>

{% endblock %}