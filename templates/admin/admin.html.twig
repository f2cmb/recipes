<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Administration{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="{{ bootstrap.css_url }}" rel="stylesheet">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
    <nav class="navbar bg-primary navbar-expand-lg navbar-dark">
        <div class="container-fluid">
            <a href="{{ path('admin.recipe.index') }}" class="navbar-brand mb-0 h1">Administration</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link {{ app.current_route starts with 'admin.recipe.'  ? '' }}" href="{{ path('admin.recipe.index') }}">Recettes</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="wrapper w-50 m-auto">
        {{ include ('partials/flash_messages.html.twig') }}

        {% block content %}
        {% endblock %}
        {% block body %}
        {% endblock %}
    </div>
    </body>
</html>
